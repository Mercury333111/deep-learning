# 2D扩散模型实验

##### 2311788

###### 南亚宏

---

**实验要求**：

掌握扩散模型的基本原理；

学会使用PyTorch搭建简单的MLP网络；

学会使用PyTorch实现扩散模型的训练和采样代码；

学会使用PyTorch运行扩散模型的训练和测试，并得到生成的数据和指标结果。

---

**实验过程**

切换三种训练目标：修改代码末尾 `train_mode = "eps"/"x0"/"v"` 分别跑三组实验

修改 MLP 宽度：`Model(hidden_dim=64/128/256)`，数值越大网络越宽

修改训练轮次：`epochs=5000/10000/15000`，控制训练深度

修改网络深度：增删 MLP 里`Linear+Softplus`层数

---

**实验结果**

`eps`目标

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-06-04-17-08-14-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-06-04-17-07-52-image.png)

`x0`目标

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-06-04-17-30-08-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-06-04-17-30-25-image.png)

`v`目标

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-06-04-17-05-27-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-06-04-17-05-43-image.png)

---

**三种训练目标对比**（固定 5 层 MLP，hidden=128，epoch=10000）

| 训练目标         | MMD 指标 | Chamfer 距离 | 生成效果              |
| ------------ | ------ | ---------- | ----------------- |
| \(\epsilon\) | 偏低     | 中等         | 螺旋结构完整，少量零散噪点     |
| \(x_0\)      | 偏高     | 偏大         | 边缘散乱，局部螺旋断裂，拟合难度高 |
| v            | 最优     | 最小         | 螺旋顺滑，和原图分布几乎重合    |

1. **eps（噪声预测）**：DDPM 原生方案，加噪分布均匀，训练收敛稳定，是最通用方案；缺点采样依赖大量步数，细节少量杂点。
2. **(x0)（原图直接预测）**：\(x_t\)含大量噪声，直接还原原图映射难度大，训练容易欠拟合，生成点离散，指标最差。
3. **v（速度预测）**：融合原图与噪声信息，训练梯度更平滑，对扩散步长鲁棒性强，收敛最快、生成质量最优，MMD 与倒角距离最优。

---

#### 其他：MLP 网络结构影响（以 v 目标为例）

1. **网络宽度（hidden_dim）**
   
   - hidden=64（窄网络）：拟合能力不足，螺旋残缺，MMD、CD 大幅上升；
   - hidden=128（基准）：结构完整，指标优秀；
   - hidden=256（宽网络）：生成效果小幅提升，训练变慢，轻微过拟合出现孤立散点。

2. **网络深度**
   
   - 少于 5 层（3 层）：表征能力不足，螺旋变形严重；
   - 5 层（实验要求）：平衡拟合与训练速度；
   - 7 层：参数量暴涨，收敛变慢，易梯度爆炸。

#### 其他：训练轮次 epoch 影响

- epoch=5000：欠拟合，螺旋残缺，大量随机散点；
- epoch=10000：收敛完成，生成正常螺旋；
- epoch=15000：小幅过拟合，生成分布局限、细节固化。

---

#### 实验心得

1. **训练目标选择**：v 预测综合效果优于eps、x0，工程中优先 v 型损失；直接预测原图\(x_0\)训练难度最大，不适合基础扩散任务。
2. **网络宽度深度**：MLP 宽度不足会导致欠拟合，过大易过拟合；5 层深度是本次 2D 小数据集最优结构，过深无益。
3. **训练轮次**：轮次过少欠拟合、过多过拟合，针对 2D 螺旋数据集 10000 轮是最优平衡点。
4. **扩散模型理解**：前向过程是可控高斯加噪，反向依靠神经网络逐步降噪，不同训练目标本质是变换优化空间，从而改变收敛难度与生成性能。

**总结：**

本实验基于 5 层 MLP 完成 2D 螺旋 DDPM 实现，验证三种损失函数、网络超参对生成的影响；**v 损失 >ϵ 损失 > x0 损失**，合理的网络规模与训练轮次是扩散模型生成高质量样本的关键。


