# 循环神经网络实验报告

##### 2311788

###### 南亚宏

---

实验要求：

掌握RNN原理

学会使用PyTorch搭建循环神经网络来训练名字识别

学会使用PyTorch搭建LSTM网络来训练名字识别

---

实验内容：

**原始 RNN 网络结构：**

```
RNN(
  (i2h): Linear(in_features=188, out_features=128, bias=True)
  (i2o): Linear(in_features=188, out_features=18, bias=True)
  (softmax): LogSoftmax(dim=1)
)
```

RNN训练结果：

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-06-04-10-14-59-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-06-04-10-15-26-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-06-04-10-15-46-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-06-04-10-16-35-image.png)



**LSTM 网络结构：**

```
LSTM(
  (rnn): LSTM(57, 64, batch_first=True)
  (out): Linear(in_features=64, out_features=18, bias=True)
  (softmax): LogSoftmax(dim=-1)
)
```

LSTM训练结果：

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-06-04-10-19-20-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-06-04-10-19-44-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-06-04-10-19-59-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-06-04-10-20-32-image.png)

---

**结果分析：**

##### LSTM 性能优于 RNN 的核心原因

1. **梯度消失 / 爆炸问题**
- **原始 RNN**：采用简单的线性循环结构，梯度在反向传播时会通过连乘传递。当序列较长（如名字长度 > 10）时，梯度会快速趋近于 0（梯度消失）或无穷大（梯度爆炸），导致模型无法学习长距离依赖。

- **LSTM**：通过门控机制（输入门、遗忘门、输出门）和细胞状态（Cell State）解决梯度消失问题：
  
  - 遗忘门：控制是否保留历史细胞状态，避免旧信息被快速遗忘；
  - 输入门：控制新信息的融入比例；
  - 细胞状态：通过线性相加（而非连乘）传递梯度，梯度能稳定传递到远距离节点。
2. **长距离依赖捕捉能力**

名字识别任务中，名字的后缀 / 前缀往往是语言特征的关键（如 “ski” 是斯拉夫语系特征、“ovich” 是俄语特征）：

- RNN：难以捕捉长名字的后缀 / 前缀与语言的关联（如 “Schwarzenegger” 的 “ger” 后缀对应德语）；
- LSTM：通过细胞状态长期存储关键特征，即使名字长度较长，仍能准确关联后缀 / 前缀与语言类别。

在实验中的表现：

损失曲线 RNN 损失下降缓慢，且易出现震荡；LSTM 损失下降更快，收敛值更低。

混淆矩阵 LSTM 有效色块集中在主对角线，RNN 混淆矩阵有部分亮色偏离对角线、跨行列连片错分；LSTM 依靠细胞状态 + 门控，牢牢记住名字首尾关键特征，缓解 RNN 梯度消失缺陷，能提取长名字前缀、后缀特征，所以混淆矩阵对角线亮度更高。小样本语种（左侧深色列）受训练数据稀缺限制仍存在识别短板，但整体分类能力超越传统 RNN；后续应扩充小语种训练样本、调整 LSTM 隐藏层维度进一步降低跨语种混淆。
