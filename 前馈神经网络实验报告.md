# 前馈神经网络实验报告

###### 2311788

###### 南亚宏

---

**前馈神经网络（FFN）的基本原理：**

FFN是一种信息单向传播的人工神经网络，数据从输入层经过一层或多层隐藏层逐层向前传递至输出层，网络中不存在循环连接；其核心计算单元是神经元，每个神经元通过权重矩阵和偏置向量对输入进行线性变换后，再经非线性激活函数（如ReLU）处理，使网络具备拟合复杂函数的能力；相邻层之间通常采用全连接方式，深层网络能够逐层提取从低级到高级的数据特征；训练时通过反向传播算法计算梯度，不断调整权重参数以最小化预测误差，从而实现对输入数据到输出目标之间的非线性映射关系的学习。

---

**实验内容：**

运行原始版本MLP，查看网络结构、损失和准确度曲线。

代码是基于 PyTorch 的 MNIST 手写数字分类任务实现，覆盖了从数据加载到模型训练、验证、可视化的全流程，核心能力包括：

1. **数据处理**：
   
   - 自动下载 MNIST 数据集，划分训练集 / 验证集（测试集）；
   - 用`DataLoader`实现批量加载，支持 GPU 加速（自动判断 CUDA 是否可用）。

2. **模型构建**：
   
   - 定义了三层全连接神经网络（含 Dropout 正则化，防止过拟合）；
   - 激活函数用 ReLU，输出层用`log_softmax`适配分类任务。

3. **训练流程**：
   
   - 用 SGD 优化器（带动量）、交叉熵损失函数；
   - 实现训练循环，打印训练过程的 loss；
   - 验证环节计算验证集 loss 和准确率。

4. **结果可视化**：
   
   - 训练前展示前 10 个样本的图像和标签；
   - 训练后绘制验证集 loss 曲线、准确率曲线。

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-04-16-20-52-25-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-04-16-20-54-25-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-04-16-20-53-08-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-04-16-20-53-31-image.png)

结果如上图中所示。

调整一系列参数，尝试提升准确度

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-04-19-21-43-17-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-04-19-21-42-51-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-04-19-21-44-14-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-04-19-21-44-34-image.png)

经过调试，准确率提升为98%。

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-04-19-21-45-03-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-04-19-21-45-20-image.png)

![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-04-19-21-45-36-image.png)



![](C:\Users\Macro\AppData\Roaming\marktext\images\2026-04-19-21-45-53-image.png)

结果如上图。

---

**分析总结：**

扩大隐藏层神经元数量、增加网络深度，模型能学习到更丰富的数字特征，分类能力明显提升。但单纯加深加宽会带来过拟合风险，因此必须配合正则化使用。

提高 Dropout 比例并加入 L2 正则化，模型在测试集上的表现更稳定，不会出现 “训练集准确率高、测试集准确率低” 的过拟合现象，泛化能力显著增强。

从 SGD 切换为 Adam 后，模型收敛速度明显加快，前期损失下降更迅速，且训练过程更平稳，不再出现 loss 剧烈波动的情况，证明自适应优化器更适合中等规模的 MLP 模型。

固定学习率在训练后期会导致模型无法精细优化，而加入学习率衰减后，模型能在后期小幅调整参数，进一步提升准确率；适当增加训练轮数可让模型充分收敛，达到更高精度。

对输入数据做均值和标准差归一化后，模型训练速度更快，loss 下降更平滑，避免因数据分布不均导致的训练不稳定。

增大 batch size 使梯度估计更稳定，模型更新更平滑，同时提升训练速度，但过大的批次会消耗更多内存，因此需要在效率与性能间平衡。

*总结：**适当增加网络深度与宽度可提升特征表达能力，Adam 优化器与学习率调度能加速收敛并提高精度，Dropout + L2 正则化共同使用有效防止过拟合，数据标准化与合理批次设置能让训练更稳定高效**。*
